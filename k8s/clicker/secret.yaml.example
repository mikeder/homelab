apiVersion: v1
kind: Secret
metadata:
  name: clicker-secret
  namespace: homelab
type: Opaque
stringData:
  POSTGRES_PASSWORD: "change-me"
  # Any additional env vars the clicker app reads (from its .env file)
